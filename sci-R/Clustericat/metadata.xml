<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<longdescription>
		This package performs clustering for categorical dataset. // This
		package performs clustering for categorical data-set by the
		block model extension of the latent class model. It contains a
		function clustericat() performing the clustering on categorical
		data-set and returns an object of appropriate class (refer to
		documentation of clustcat). The package also provides tool
		functions like summary(), summary_dependencies() and plot() to
		summarize results, to present the main conditional
		dependencies, to visualize the parameters respectively.
	</longdescription>
</pkgmetadata>
